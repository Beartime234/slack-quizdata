version: 0.2

phases:
  install:
    commands:
      - echo -- Installing Environment --
      - pipenv install
  build:
    commands:
      - echo -- Running Test Commands --
      - pipenv run python -m uploader.test